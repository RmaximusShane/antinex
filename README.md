# a<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>AntiNex TechX | Sovereign Photonics</title>
    <style>
        body { background-color: #02060f; color: #a0aec0; font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; margin: 0; line-height: 1.6; }
        .hero { height: 100vh; display: flex; flex-direction: column; justify-content: center; align-items: center; border-bottom: 1px solid #1a202c; text-align: center; }
        h1 { color: #f6ad55; font-size: 4rem; letter-spacing: 10px; margin-bottom: 0; }
        .alert { color: #f56565; border: 1px solid #f56565; padding: 5px 15px; font-size: 0.8rem; margin-top: 20px; animation: pulse 2s infinite; }
        @keyframes pulse { 0% { opacity: 1; } 50% { opacity: 0.3; } 100% { opacity: 1; } }
        .specs { display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 20px; padding: 50px; }
        .card { background: #0d1117; padding: 25px; border: 1px solid #30363d; border-radius: 8px; }
        .card h3 { color: #63b3ed; margin-top: 0; }
    </style>
</head>
<body>
    <div class="hero">
        <h1>ANTINEX TECHX</h1>
        <p>Architect: Adekoya Jotham</p>
        <div class="alert">HARDWARE SECURITY PROTOCOL: ACTIVE</div>
        <p style="max-width: 600px; margin-top: 30px;">Advancing the frontier of 65nm Photonic Computing. Home of the FemeX Prime Sovereign Chip.</p>
    </div>
    <div class="specs">
        <div class="card"><h3>Sextabyte Density</h3><p>3D Wavelength-Stacked Optical RAM.</p></div>
        <div class="card"><h3>Guardian AI</h3><p>Femtosecond Latency Threat Detection.</p></div>
        <div class="card"><h3>Logic Melt</h3><p>Physical self-destruction via PCM shift.</p></div>
    </div>
</body>
</html>ntinex
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AntiNex TechX | Sovereign Photonic Architecture</title>
    <style>
        :root {
            --bg: #020617;
            --accent-gold: #fde047;
            --accent-cyan: #22d3ee;
            --danger: #ef4444;
            --glass: rgba(30, 41, 59, 0.7);
        }

        body {
            background-color: var(--bg);
            color: #f8fafc;
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            margin: 0;
            overflow-x: hidden;
        }

        /* Hero Section */
        .hero {
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            background: radial-gradient(circle at center, #1e293b 0%, #020617 70%);
            text-align: center;
            border-bottom: 2px solid #1e293b;
        }

        h1 {
            color: var(--accent-gold);
            font-size: clamp(3rem, 10vw, 6rem);
            font-weight: 900;
            letter-spacing: -2px;
            margin: 0;
            text-shadow: 0 0 30px rgba(253, 224, 71, 0.3);
        }

        .subtitle {
            color: var(--accent-cyan);
            font-family: 'monospace';
            font-size: 1.2rem;
            letter-spacing: 4px;
            margin-top: 10px;
            text-transform: uppercase;
        }

        /* Blinking Alert */
        .status-bar {
            margin-top: 30px;
            padding: 8px 20px;
            border: 1px solid var(--danger);
            color: var(--danger);
            font-size: 0.8rem;
            font-weight: bold;
            background: rgba(239, 68, 68, 0.1);
            animation: blink 1.5s infinite;
        }

        @keyframes blink {
            50% { opacity: 0.3; }
        }

        /* Tech Grid */
        .specs {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 25px;
            padding: 80px 10%;
            background: #020617;
        }

        .card {
            background: var(--glass);
            padding: 40px;
            border: 1px solid #334155;
            border-radius: 12px;
            transition: transform 0.3s ease, border-color 0.3s ease;
        }

        .card:hover {
            transform: translateY(-10px);
            border-color: var(--accent-cyan);
        }

        .card h3 {
            color: var(--accent-cyan);
            font-size: 1.5rem;
            margin-top: 0;
        }

        .card p {
            color: #94a3b8;
            line-height: 1.6;
        }

        /* Footer */
        footer {
            padding: 60px;
            text-align: center;
            border-top: 1px solid #1e293b;
            font-size: 0.9rem;
            color: #475569;
        }

        .highlight { color: var(--accent-gold); }
    </style>
</head>
<body>

    <section class="hero">
        <h1>ANTINEX <span style="color:white">TECHX</span></h1>
        <p class="subtitle">Sovereign Photonic Systems</p>
        <div class="status-bar">PROTECTION LEVEL: SOVEREIGN GRADE</div>
        <p style="margin-top: 40px; color: #94a3b8;">Lead Architect: <span class="highlight">Adekoya Jotham</span></p>
    </section>

    <div class="specs">
        <div class="card">
            <h3>Sextabyte Density</h3>
            <p>128-channel spectral stacking using 3D photonic crystals. Moving beyond planar limits.</p>
        </div>
        <div class="card">
            <h3>Guardian AI</h3>
            <p>Real-time femtosecond latency fingerprinting. Immediate detection of signal anomalies.</p>
        </div>
        <div class="card">
            <h3>Amorphous Scramble</h3>
            <p>Physical logic destruction via thermal PCM transition. Data security at the atomic level.</p>
        </div>
        <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AntiNex TechX | Sovereign Photonic Architecture</title>
    <style>
        :root {
            --bg: #020617;
            --accent-gold: #fde047;
            --accent-cyan: #22d3ee;
            --danger: #ef4444;
            --glass: rgba(15, 23, 42, 0.8);
        }

        body {
            background-color: var(--bg);
            color: #f8fafc;
            font-family: 'Courier New', Courier, monospace;
            margin: 0;
            overflow-x: hidden;
        }

        /* Live Background Canvas */
        #photonicCanvas {
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            z-index: -1;
            opacity: 0.6;
        }

        /* Hero Section */
        .hero {
            min-height: 80vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 20px;
        }

        h1 {
            color: var(--accent-gold);
            font-size: clamp(3rem, 8vw, 5rem);
            font-weight: 900;
            letter-spacing: -2px;
            margin: 0;
            text-shadow: 0 0 40px rgba(253, 224, 71, 0.4);
        }

        .subtitle {
            color: var(--accent-cyan);
            font-size: 1.2rem;
            letter-spacing: 4px;
            margin-top: 10px;
            text-transform: uppercase;
        }

        .status-bar {
            margin-top: 30px;
            padding: 10px 25px;
            border: 1px solid var(--danger);
            color: var(--danger);
            font-size: 0.9rem;
            font-weight: bold;
            background: rgba(239, 68, 68, 0.1);
            animation: blink 1.2s infinite;
            box-shadow: 0 0 15px rgba(239, 68, 68, 0.3);
        }

        @keyframes blink { 50% { opacity: 0.4; } }

        /* The Simulation Viewer (Where your MP4 goes) */
        .video-container {
            max-width: 900px;
            margin: 0 auto 80px auto;
            padding: 20px;
            background: var(--glass);
            border: 1px solid var(--accent-cyan);
            border-radius: 8px;
            box-shadow: 0 0 30px rgba(34, 211, 238, 0.1);
            text-align: center;
        }

        .video-container h2 {
            color: var(--accent-gold);
            margin-top: 0;
        }

        video {
            width: 100%;
            border-radius: 4px;
            border: 1px solid #334155;
            background: #000;
        }

        /* Tech Grid */
        .specs {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 25px;
            padding: 40px 10%;
            max-width: 1200px;
            margin: auto;
        }

        .card {
            background: var(--glass);
            padding: 30px;
            border: 1px solid #334155;
            border-left: 4px solid var(--accent-cyan);
            transition: all 0.3s ease;
            backdrop-filter: blur(10px);
        }

        .card:hover {
            transform: scale(1.02);
            border-left-color: var(--accent-gold);
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
        }

        .card h3 { color: var(--accent-cyan); margin-top: 0; }
        .card p { color: #94a3b8; line-height: 1.6; }

        footer {
            padding: 60px;
            text-align: center;
            font-size: 0.9rem;
            color: #475569;
            background: #020617;
            border-top: 1px solid #1e293b;
        }

        .highlight { color: var(--accent-gold); }
    </style>
</head>
<body>

    <canvas id="photonicCanvas"></canvas>

    <section class="hero">
        <h1>ANTINEX <span style="color:white">TECHX</span></h1>
        <p class="subtitle">Sovereign Photonic Systems</p>
        <div class="status-bar">PROTECTION LEVEL: SOVEREIGN GRADE - ACTIVE</div>
        <p style="margin-top: 40px; color: #94a3b8;">Lead Architect: <span class="highlight">Adekoya Jotham</span></p>
    </section>

    <div class="video-container">
        <h2>FemeX Prime V2.0 : Architecture Simulation</h2>
        <p style="color: #94a3b8; margin-bottom: 20px;">Live visualization of Wavelength Multiplexing and Guardian AI Amorphous Melt Sequence.</p>
        <video controls autoplay loop muted>
            <source src="FemeX_Omninexus_Mega_Simulation.mp4" type="video/mp4">
            Your browser does not support the video tag.
        </video>
    </div>

    <div class="specs">
        <div class="card">
            <h3>[ 01 ] Sextabyte Density</h3>
            <p>128-channel spectral stacking using 3D photonic crystals. Moving beyond planar electronic limits to achieve ocean-scale data density.</p>
        </div>
        <div class="card">
            <h3>[ 02 ] Guardian AI</h3>
            <p>Real-time femtosecond latency fingerprinting. Immediate detection of signal anomalies moving faster than traditional time-clocks.</p>
        </div>
        <div class="card">
            <h3>[ 03 ] Amorphous Scramble</h3>
            <p>Physical logic destruction via thermal PCM transition. Data security at the atomic level, transforming compromised chips into blank slate glass.</p>
        </div>
    </div>

    <footer>
        &copy; 2026 AntiNex TechX. Developed in Lagos, Nigeria.<br>
        Engineering the standard for zero-trust hardware.<br>
        Built for Silicon Catalyst & NVIDIA Inception.
    </footer>

    <script>
        const canvas = document.getElementById('photonicCanvas');
        const ctx = canvas.getContext('2d');
        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;

        let particlesArray = [];
        const colors = ['#22d3ee', '#fde047', '#3b82f6'];

        class Particle {
            constructor() {
                this.x = Math.random() * canvas.width;
                this.y = Math.random() * canvas.height;
                this.size = Math.random() * 2 + 1;
                this.speedX = (Math.random() * 1) - 0.5;
                this.speedY = (Math.random() * 1) - 0.5;
                this.color = colors[Math.floor(Math.random() * colors.length)];
            }
            update() {
                this.x += this.speedX;
                this.y += this.speedY;
                if (this.x > canvas.width || this.x < 0) this.speedX = -this.speedX;
                if (this.y > canvas.height || this.y < 0) this.speedY = -this.speedY;
            }
            draw() {
                ctx.fillStyle = this.color;
                ctx.beginPath();
                ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
                ctx.fill();
            }
        }

        function init() {
            particlesArray = [];
            for (let i = 0; i < 150; i++) {
                particlesArray.push(new Particle());
            }
        }

        function animate() {
            ctx.clearRect(0, 0, canvas.width, canvas.height);
            for (let i = 0; i < particlesArray.length; i++) {
                particlesArray[i].update();
                particlesArray[i].draw();
                // Draw connecting lines to simulate optical networking
                for (let j = i; j < particlesArray.length; j++) {
                    const dx = particlesArray[i].x - particlesArray[j].x;
                    const dy = particlesArray[i].y - particlesArray[j].y;
                    const distance = Math.sqrt(dx * dx + dy * dy);
                    if (distance < 80) {
                        ctx.beginPath();
                        ctx.strokeStyle = particlesArray[i].color;
                        ctx.lineWidth = 0.2;
                        ctx.moveTo(particlesArray[i].x, particlesArray[i].y);
                        ctx.lineTo(particlesArray[j].x, particlesArray[j].y);
                        ctx.stroke();
                        ctx.closePath();
                    }
                }
            }
            requestAnimationFrame(animate);
        }

        init();
        animate();

        window.addEventListener('resize', () => {
            canvas.width = window.innerWidth;
            canvas.height = window.innerHeight;
            init();
        });
    </script>
</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AntiNex TechX | Sovereign Photonic Systems</title>
    <style>
        :root {
            --bg: #020617;
            --surface: #0f172a;
            --border: #1e293b;
            --gold: #fde047;
            --gold-glow: rgba(253, 224, 71, 0.2);
            --cyan: #22d3ee;
            --cyan-glow: rgba(34, 211, 238, 0.2);
            --danger: #ef4444;
            --text-main: #f8fafc;
            --text-muted: #94a3b8;
        }

        body {
            background-color: var(--bg);
            color: var(--text-main);
            font-family: 'Inter', system-ui, -apple-system, sans-serif;
            margin: 0;
            padding: 0;
            overflow-x: hidden;
            background-image: 
                radial-gradient(circle at 50% 15%, var(--surface) 0%, transparent 60%),
                linear-gradient(to bottom, rgba(255,255,255,0.02) 1px, transparent 1px),
                linear-gradient(to right, rgba(255,255,255,0.02) 1px, transparent 1px);
            background-size: 100% 100%, 40px 40px, 40px 40px;
        }

        /* Hero Container */
        .hero {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 2rem;
            position: relative;
            border-bottom: 1px solid var(--border);
        }

        .hero-graphic {
            margin-bottom: 2rem;
            animation: float 6s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0) scale(1); }
            50% { transform: translateY(-10px) scale(1.02); }
        }

        h1 {
            font-size: clamp(2.5rem, 8vw, 5.5rem);
            font-weight: 900;
            letter-spacing: -2px;
            margin: 0;
            color: var(--text-main);
        }

        h1 span {
            color: var(--gold);
            text-shadow: 0 0 25px var(--gold-glow);
        }

        .subtitle {
            color: var(--cyan);
            font-family: monospace;
            font-size: 1.1rem;
            letter-spacing: 3px;
            text-transform: uppercase;
            margin-top: 0.5rem;
        }

        /* Dynamic Status Bar */
        .status-wrapper {
            display: inline-flex;
            align-items: center;
            gap: 12px;
            background: rgba(239, 68, 68, 0.08);
            border: 1px solid var(--danger);
            padding: 8px 24px;
            border-radius: 4px;
            margin-top: 2.5rem;
            font-family: monospace;
            font-weight: bold;
            color: var(--danger);
            letter-spacing: 1px;
        }

        .status-dot {
            width: 10px;
            height: 10px;
            background-color: var(--danger);
            border-radius: 50%;
            box-shadow: 0 0 10px var(--danger);
            animation: pulse 1.5s infinite;
        }

        @keyframes pulse {
            0%, 100% { opacity: 1; transform: scale(1); }
            50% { opacity: 0.4; transform: scale(0.8); }
        }

        /* Video Showcase Section */
        .showcase {
            max-width: 1000px;
            margin: -4rem auto 4rem;
            padding: 0 1.5rem;
            position: relative;
            z-index: 10;
        }

        .video-container {
            background: var(--surface);
            border: 1px solid var(--border);
            border-radius: 12px;
            padding: 10px;
            box-shadow: 0 20px 50px rgba(0,0,0,0.8);
            aspect-ratio: 16 / 9;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
            position: relative;
        }

        .video-container video {
            width: 100%;
            height: 100%;
            border-radius: 8px;
            object-fit: cover;
            background: #000;
        }

        /* Architecture Core Grid */
        .grid-header {
            text-align: center;
            margin: 6rem 0 3rem;
        }

        .grid-header h2 {
            color: var(--gold);
            font-size: 2rem;
            margin: 0;
        }

        .specs-grid {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 1.5rem 6rem;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2rem;
        }

        .card {
            background: rgba(15, 23, 42, 0.6);
            border: 1px solid var(--border);
            border-radius: 12px;
            padding: 2.5rem 2rem;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            backdrop-filter: blur(10px);
        }

        .card:hover {
            border-color: var(--cyan);
            transform: translateY(-5px);
            box-shadow: 0 10px 30px var(--cyan-glow);
        }

        .card-graphic {
            height: 60px;
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
        }

        .card h3 {
            color: var(--text-main);
            font-size: 1.3rem;
            margin: 0 0 1rem 0;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .card h3 span {
            color: var(--cyan);
            font-family: monospace;
            font-size: 0.9rem;
        }

        .card p {
            color: var(--text-muted);
            line-height: 1.6;
            margin: 0;
            font-size: 0.95rem;
        }

        footer {
            border-top: 1px solid var(--border);
            padding: 4rem 2rem;
            text-align: center;
            color: var(--text-muted);
            font-size: 0.9rem;
            background: rgba(2, 6, 23, 0.9);
        }

        .highlight { color: var(--gold); font-weight: bold; }
    </style>
</head>
<body>

    <header class="hero">
        <svg class="hero-graphic" width="180" height="180" viewBox="0 0 100 100" fill="none" xmlns="http://www.w3.org/2000/svg">
            <circle cx="50" cy="50" r="45" stroke="#1e293b" stroke-width="2"/>
            <circle cx="50" cy="50" r="35" stroke="#22d3ee" stroke-width="1" stroke-dasharray="4 4"/>
            <circle cx="50" cy="50" r="20" fill="#fde047" fill-opacity="0.1" stroke="#fde047" stroke-width="2"/>
            <path d="M50 5 L50 95 M5 50 L95 50" stroke="#22d3ee" stroke-width="1" stroke-opacity="0.5"/>
            <circle cx="50" cy="50" r="6" fill="#fde047"/>
            <circle cx="50" cy="20" r="4" fill="#22d3ee"/>
            <circle cx="80" cy="50" r="4" fill="#22d3ee"/>
            <circle cx="50" cy="80" r="4" fill="#22d3ee"/>
            <circle cx="20" cy="50" r="4" fill="#22d3ee"/>
        </svg>

        <h1>ANTINEX <span>TECHX</span></h1>
        <div class="subtitle">Sovereign Photonic Architecture</div>
        
        <div class="status-wrapper">
            <div class="status-dot"></div>
            <span>LOGIC INTEGRITY: SECURE</span>
        </div>

        <p style="margin-top: 2rem; color: var(--text-muted);">
            Chief Architect: <span class="highlight">Adekoya Jotham</span>
        </p>
    </header>

    <section class="showcase">
        <div class="video-container">
            <video autoplay loop muted playsinline>
                <source src="FemeX_Simulation.mp4" type="video/mp4">
                <source src="Femex_Prime_V2_Dashboard_Simulation.mp4" type="video/mp4">
                Your browser does not support the video tag.
            </video>
        </div>
    </section>

    <div class="grid-header">
        <h2>CORE SUBSYSTEMS</h2>
        <p style="color: var(--text-muted); font-family: monospace;">FemeX Prime V2.0 Hardware Specifications</p>
    </div>

    <section class="specs-grid">
        <div class="card">
            <div class="card-graphic">
                <svg width="60" height="50" viewBox="0 0 60 50" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <path d="M5 35 L30 45 L55 35 L30 25 Z" stroke="#1e293b" stroke-width="2" fill="#0f172a"/>
                    <path d="M5 25 L30 35 L55 25 L30 15 Z" stroke="#22d3ee" stroke-width="2" fill="rgba(34, 211, 238, 0.1)"/>
                    <path d="M5 15 L30 25 L55 15 L30 5 Z" stroke="#fde047" stroke-width="2" fill="rgba(253, 224, 71, 0.1)"/>
                </svg>
            </div>
            <h3>Sextabyte Index <span>[3D WDM]</span></h3>
            <p>128 discrete spectral channels multiplexed across stacked 65nm optical layers, achieving unprecedented volumetric density.</p>
        </div>

        <div class="card">
            <div class="card-graphic">
                <svg width="60" height="50" viewBox="0 0 60 50" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <rect x="10" y="10" width="40" height="30" rx="6" stroke="#22d3ee" stroke-width="2"/>
                    <path d="M20 25 h20 M30 15 v20" stroke="#fde047" stroke-width="2"/>
                    <circle cx="30" cy="25" r="4" fill="#020617" stroke="#22d3ee" stroke-width="2"/>
                </svg>
            </div>
            <h3>Guardian AI <span>[10⁻¹⁵s Tracking]</span></h3>
            <p>Real-time femtosecond latency fingerprinting isolates anomaly injection vectors at the physical transport layer.</p>
        </div>

        <div class="card">
            <div class="card-graphic">
                <svg width="60" height="50" viewBox="0 0 60 50" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <path d="M5 25 Q 15 5, 30 25 T 55 25" stroke="#ef4444" stroke-width="2" fill="none"/>
                    <path d="M5 30 Q 15 45, 30 30 T 55 30" stroke="#ef4444" stroke-width="2" stroke-dasharray="3 3" fill="none"/>
                </svg>
            </div>
            <h3>Amorphous Scramble <span>[PCM Shift]</span></h3>
            <p>Integrated thermal triggers immediately transition Y-junction logic states from crystalline to amorphous glass upon hardware intrusion.</p>
        </div>

        <div class="card">
            <div class="card-graphic">
                <svg width="60" height="50" viewBox="0 0 60 50" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <circle cx="30" cy="25" r="18" stroke="#fde047" stroke-width="2" stroke-dasharray="6 4"/>
                    <circle cx="30" cy="25" r="6" fill="#22d3ee"/>
                    <path d="M30 25 L45 10" stroke="#fde047" stroke-width="1"/>
                </svg>
            </div>
            <h3>C-14 Power Guard <span>[3.3V Stable]</span></h3>
            <section class="hero">
    <img src="logo.png" alt="AntiNex TechX Logo" style="width: 150px; height: 150px; margin-bottom: 20px;">
    <h1>ANTINEX <span style="color:white">TECHX</span></h1>
    ...
</section>

            <p>Continuous atomic-decay power stabilization delivers consistent current to defensive logical arrays across a 50-year operational lifecycle.</p>
        </div>
    </section>

    <footer>
        <p>&copy; 2026 AntiNex TechX. Engineered in Lagos, Nigeria.</p>
        <p style="margin-top: 10px; color: var(--border);">GDSII System Master Finalized &bull; NVIDIA Inception Application Network Ready</p>
    </footer>

</body>
</html>
    </div>

    <footer>
        &copy; 2026 AntiNex TechX. Developed in Lagos, Nigeria. <br>
        Built for Silicon Catalyst & NVIDIA Inception.
    </footer>

</body>
</html>
